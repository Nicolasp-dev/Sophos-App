<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SophosApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">


    </style>

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- 1. Icon item || 2.  -->
        <item name="colorControlNormal">@color/light_purple</item>
        <item name="titleTextColor">@color/light_purple</item>
    </style>

    <style name="FingerPrintButton" parent="ThemeOverlay.Material3.Button">
        <!-- 1. Icon item || 2.  -->
        <item name="android:textColor">@color/light_purple</item>
        <item name="strokeColor">@color/light_purple</item>
        <item name="strokeWidth">@dimen/btnBorderStroke</item>
        <item name="backgroundTint">@color/white</item>
        <item name="icon">@drawable/ic_fingerprint</item>
        <item name="iconTint">@color/light_purple</item>

    </style>



</resources>